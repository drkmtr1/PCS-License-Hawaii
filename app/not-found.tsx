export default function NotFound() {
  return (
    <main className="page-shell">
      <div className="page-content">
        <p className="eyebrow">PCS License Hawaiʻi</p>
        <h1>That page is not available.</h1>
        <section className="status-card" aria-labelledby="status-heading">
          <h2 className="status-label" id="status-heading">Prototype status</h2>
          <p>This prototype does not provide licensing advice or determine authorization to practice.</p>
        </section>
      </div>
    </main>
  );
}
